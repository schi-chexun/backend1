# 项目结构说明

## 📁 重构后的项目结构

```
src/main/java/com/easyactivity/
├── EasyActivityApplication.java          # 主启动类
│
├── common/                               # 通用类
│   ├── enums/                            # 枚举类
│   │   ├── ActivityStatusEnum.java       # 活动状态枚举
│   │   └── UserRoleEnum.java             # 用户角色枚举
│   ├── GlobalExceptionHandler.java      # 全局异常处理
│   └── Result.java                       # 统一响应结果
│
├── config/                               # 配置类
│   ├── MybatisConfig.java               # MyBatis配置
│   └── WebMvcConfig.java                # Web MVC配置（跨域、拦截器）
│
├── controller/                          # 控制器层
│   ├── ActivityController.java           # 活动控制器
│   ├── ActivityParticipantController.java # 参与者控制器
│   ├── AdminController.java             # 管理员控制器
│   └── UserController.java              # 用户控制器
│
├── dto/                                  # 数据传输对象（请求）
│   ├── ActivityPublishDTO.java          # 活动发布DTO
│   ├── LoginDTO.java                     # 登录DTO
│   └── RegisterDTO.java                  # 注册DTO
│
├── entity/                               # 实体类
│   ├── Activity.java                     # 活动实体
│   ├── ActivityParticipant.java         # 参与者实体
│   ├── AdminStats.java                   # 管理员统计
│   ├── AuditLog.java                     # 审计日志
│   └── User.java                         # 用户实体
│
├── interceptor/                          # 拦截器
│   ├── AdminInterceptor.java             # 管理员拦截器
│   └── LoginInterceptor.java             # 登录拦截器
│
├── mapper/                               # Mapper接口
│   ├── ActivityMapper.java
│   ├── ActivityParticipantMapper.java
│   ├── AdminMapper.java
│   └── UserMapper.java
│
├── service/                              # 服务层接口
│   ├── ActivityService.java
│   ├── ActivityParticipantService.java
│   ├── AdminService.java
│   └── UserService.java
│
├── service/impl/                         # 服务层实现
│   ├── ActivityServiceImpl.java
│   ├── ActivityParticipantServiceImpl.java
│   ├── AdminServiceImpl.java
│   └── UserServiceImpl.java
│
├── utils/                                # 工具类
│   └── UserContext.java                  # 用户上下文
│
└── vo/                                   # 视图对象（响应）
    ├── ActivityVO.java                   # 活动视图对象
    └── UserVO.java                       # 用户视图对象
```

## 🔄 重构内容

### 1. Service 层分离
- ✅ 将 Service 实现类移到 `service/impl` 包
- ✅ Service 接口保留在 `service` 包
- ✅ 符合面向接口编程原则

### 2. DTO（数据传输对象）
- ✅ `ActivityPublishDTO` - 活动发布请求
- ✅ `RegisterDTO` - 用户注册请求
- ✅ `LoginDTO` - 用户登录请求

### 3. VO（视图对象）
- ✅ `ActivityVO` - 活动响应对象
- ✅ `UserVO` - 用户响应对象（不包含密码）

### 4. 工具类
- ✅ `UserContext` - 用户上下文工具类（ThreadLocal）

### 5. 枚举类
- ✅ `ActivityStatusEnum` - 活动状态枚举
- ✅ `UserRoleEnum` - 用户角色枚举

### 6. 配置文件
- ✅ 将 `application.properties` 改为 `application.yml`
- ✅ 配置更清晰易读

### 7. 配置类
- ✅ `MybatisConfig` - MyBatis 配置
- ✅ `WebMvcConfig` - Web MVC 配置（替代原来的 WebConfig 和 CorsConfig）

## 🚀 运行项目

### 1. 确保数据库已创建
执行 `src/main/resources/sql/schema.sql` 创建数据库和表

### 2. 检查配置文件
确认 `src/main/resources/application.yml` 中的数据库配置正确：
- 用户名：`root`
- 密码：`zhx1104`
- 数据库：`easy_activity`

### 3. 编译项目
```bash
.\mvnw.cmd clean compile
```

### 4. 运行项目
```bash
.\mvnw.cmd spring-boot:run
```

### 5. 测试 API
- 获取所有活动：`http://localhost:8080/api/activities`
- 用户注册：`POST http://localhost:8080/api/users/register`
- 用户登录：`POST http://localhost:8080/api/users/login`

## ✅ 重构完成

项目已按照标准结构重构完成，所有代码已编译通过，可以正常运行！

